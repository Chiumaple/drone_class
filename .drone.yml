pipeline:

  backend-1:
    image: golang
    group: build
    commands:
      - echo "sleep 10"
      - sleep 10
      
  test-1:
    image: golang
    group: build
    commands:
      - echo "test other"
      - touch a.txt
      - ls
